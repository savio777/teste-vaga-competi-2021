# Projeto teste de vaga Frontend COMPETI

### Para rodar o projeto:

- instale as dependências do projeto com o comando: `yarn` ou `npm install`
- após a instalação dos pacotes rode o comando: `yarn start` ou `npm start`
